# Videos JSON (file update demo)

A small Go program that:

- reads `videos.json`
- modifies each video’s `description`
- writes the updated list to `videos-updated.json`

## Run

From `videos-json/`:

```bash
go run .
```

After running, check:

- `videos-updated.json`


